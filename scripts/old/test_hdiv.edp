

mesh Th = square(10,10);
plot(Th);
fespace Hdiv(Th, RT0);

Hdiv [Bx, By] = [1, 1];

for (int k = 0; k < Th.nt; k++)
  cout << Bx[][Hdiv(k,0)] << " " << Bx[][Hdiv(k,1)] << " " << Bx[][Hdiv(k,2)] << endl;

real[int] JfluxRe(Hdiv.ndof);

cout << Hdiv.ndof << endl;